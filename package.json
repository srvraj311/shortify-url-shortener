{
  "name": "url-shortner-fullstack",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "build": "cd client && npm run build",
    "heroku-postbuild" : "npm run install-client && npm install -g @angular/cli && npm run build",
    "install-client": "cd client && npm install"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "mongoose": "^6.3.0",
    "morgan": "~1.9.1",
    "validator": "^13.7.0"
  }
}
